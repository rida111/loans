spring:
  datasource:
    url: jdbc:mysql://localhost:3306/loans_db?useSSL=false&serverTimezone=UTC
    username: testingUser
    password: testing
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

  sql:
    init:
      mode: always